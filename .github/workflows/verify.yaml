name: Verify
on: [push]

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Shell syntax check
        run: bash -n maintaining_dialog.sh
      - name: Shellcheck
        uses: ludeeus/action-shellcheck@master
        with:
          files: maintaining_dialog.sh
